import _ from "jsonwebtoken";
import { RequestHandler } from "express";

const decode: RequestHandler = (_, __, ___) => {};

const encode: RequestHandler = (_, __, ___) => {};

export { decode, encode };
