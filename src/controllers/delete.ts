import { RequestHandler } from "express";

const deleteRoomById: RequestHandler = (_, __) => {};

const deleteMessageById: RequestHandler = (_, __) => {};

export { deleteRoomById, deleteMessageById };
